spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://users-database/users
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
